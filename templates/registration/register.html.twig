{% extends 'base.html.twig' %}

{% block title %}S'inscrire{% endblock %}

{% block body %}
{% include 'navigation.html.twig' %}
<section class="vh-100" style="background-color: #eee;">
  <div class="container h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col-lg-12 col-xl-11">
        <div class="card text-black" style="border-radius: 25px;">
          <div class="card-body p-md-5">
            <div class="row justify-content-center">
              <div class="col-md-10 col-lg-6 col-xl-5 order-2 order-lg-1">
                    <p class="text-center h1 fw-bold mb-5 mx-1 mx-md-4 mt-4">S'inscrire</p> 
                        
    
                        {# <form class="mx-1 mx-md-4"> #}
                          {{ form_errors(registrationForm) }}

                          {{ form_start(registrationForm, {'attr':{'class':'mx-1 mx-md-4'}}) }}
                              {{ form_row(registrationForm.nom) }}
                              {{ form_row(registrationForm.email) }}
                              {{ form_row(registrationForm.plainPassword, {
                                  label: 'Password'
                              }) }}
                              {{ form_row(registrationForm.agreeTerms) }}

                              <button type="submit" class="btn">Register</button>
                          {{ form_end(registrationForm) }}
                        {# </form> #}
{# 

                    {{ form_errors(registrationForm) }}

                    {{ form_start(registrationForm) }}
                        <form class="mx-1 mx-md-4">
                                    
                                    <div class="d-flex flex-row align-items-center mb-4">
                                        <i class="fas fa-user fa-lg me-3 fa-fw"></i>
                                        <div class="form-outline flex-fill mb-0">
                                            {{ form_row(registrationForm.email, {'attr':{'class':'form-control','id':'form3Example1c'}}) }}
                                        </div>
                                    </div>

                                    <div class="d-flex flex-row align-items-center mb-4">
                                        <i class="fas fa-user fa-lg me-3 fa-fw"></i>
                                        <div class="form-outline flex-fill mb-0">
                                
                                            {{ form_row(registrationForm.plainPassword, {label: 'Password', 'attr':{'class':'form-control','id':'form3Example1c'}}) }}
                                            
                                        </div>
                                    </div>
                                  
                                     <div class="form-check d-flex justify-content-center mb-5">
                                        {{ form_row(registrationForm.agreeTerms,{'attr':{'class':'form-check-input me-2 form-control', 'type':'checkbox'}}) }} 
                                       
                                    </div>
                                    
                                     <div class="d-flex justify-content-center mx-4 mb-3 mb-lg-4">
                                       <button type="submit" class="btn btn-primary btn-lg">Register</button>
                                    </div>


                                    {# <button type="submit" class="btn">Register</button> #}
                        {# </form> #}
                    {# {{ form_end(registrationForm) }} #} 
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
{% endblock %}
